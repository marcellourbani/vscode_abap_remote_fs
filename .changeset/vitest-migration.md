---
"vscode-abap-remote-fs": patch
---

Replace the Jest test runner with named Vitest projects across the client, server, and workspace modules while preserving the existing test coverage and behavior.
